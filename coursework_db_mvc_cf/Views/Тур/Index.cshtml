@model IEnumerable<coursework_db_mvc_cf.Models.DB.Тур>

@{
    ViewBag.Title = "Index";
}

<h2>Список туров</h2>

<p>
    @Html.ActionLink("Добавить", "Create")
</p>


@foreach (var item in Model) {
    <div class="card">
        <div class="card-header">
            @Html.DisplayFor(modelItem => item.Место_отдыха.Название)
        </div>
        <div class="card-body">
            <h5 class="card-title">Special title treatment</h5>
            <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
            @Html.ActionLink("Изменить", "Edit", new { id = item.ИД }, new { @class = "btn btn-primary" }) |
            @Html.ActionLink("Удалить", "Delete", new { id = item.ИД }, new { @class = "btn btn-primary" })
        </div>
    </div>
    @*<tr>
        <td>
            
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ночёвка.Тип_номера)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Вкл_гида)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Вкл_питание)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Вкл_ночёвка)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Вкл_поездка)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Общая_стоимость)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Длительность_отдыха_в_днях)
        </td>
    </tr>*@
}
