@model IEnumerable<coursework_db_mvc_cf.Models.DB.Отель>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ИД_Адреса)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Название_отели)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Рейтинг)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Индекс)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ИД_Адреса)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Название_отели)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Рейтинг)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Индекс)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ИД }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ИД })
        </td>
    </tr>
}

</table>
